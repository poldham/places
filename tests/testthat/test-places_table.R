# context("test-places_table.R")
# # not presently working because of problem loading data
# test_that("fun returns data frame", {
#   table <- places_table(countryname = NULL)
#   expect_is(table, class = "data.frame")
#   expect_length(table, 9)
# })
#
# test_that("fun returns data frame with country name", {
#   table <- places_table(countryname = TRUE)
#   expect_is(table, class = "data.frame")
#   expect_length(table, 11)
# })

# add region table

